pygame==2.6.1
moviepy>=2.0.0
imageio>=2.31.1
imageio-ffmpeg>=0.4.8
numpy>=2.0.0
proglog>=0.1.10
tqdm>=4.66
pyinstaller>=6.3.0
decorator>=5.0.0
requests>=2.31.0

# Dependências de vídeo (evitam modo fallback na abertura)
# (já incluídas acima)

# Testes
pytest>=7.0.0
pytest-cov>=4.0.0

# Dependências para build móvel (opcionais)
# Descomente as linhas abaixo se for fazer build para mobile
# buildozer>=1.5.0
# kivy>=2.1.0
# cython>=0.29.0
# python-for-android>=2023.0.0
# kivy-ios>=1.3.0