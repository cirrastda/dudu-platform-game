# Dependências para Build Móvel - Jump and Hit
# Instale com: pip install -r requirements-mobile.txt

# Dependências base (já devem estar instaladas)
pygame>=2.5.0

# Build para Android
buildozer>=1.5.0
cython>=0.29.0
python-for-android>=2023.0.0

# Build para iOS (apenas macOS)
# kivy-ios>=1.3.0

# Framework alternativo (recomendado para mobile)
# kivy>=2.1.0
# kivymd>=1.1.0

# Ferramentas de desenvolvimento
colorama>=0.4.0  # Para cores no terminal
requests>=2.28.0  # Para downloads automáticos

# Dependências opcionais para otimização
# pillow>=9.0.0  # Para processamento de imagens
# pydub>=0.25.0  # Para processamento de áudio